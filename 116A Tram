#include <iostream>
#include <vector>
using namespace std;

int main() {

	int n, a, b;
	int counter = 0, max = 0;
	cin >> n;
	for (int i = 0;i < n;i++) {
		cin >> a >> b;
		counter -= a;
		counter += b;
		if (counter > max)max = counter;
	}
	cout << max;

	return 0;
}


